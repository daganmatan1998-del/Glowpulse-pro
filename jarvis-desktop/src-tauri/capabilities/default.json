{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The panel, plus opening a URL in the default browser. Still no filesystem and no shell. open-url is the narrowest useful capability there is: it hands a link to the browser and can do nothing else, and the page restricts it to http and https before it is ever called.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-set-position",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-center",
    "core:window:allow-start-dragging",
    "opener:default",
    "opener:allow-open-url",
    "global-shortcut:allow-register",
    "global-shortcut:allow-unregister",
    "global-shortcut:allow-is-registered"
  ]
}
